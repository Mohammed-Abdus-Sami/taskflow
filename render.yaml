services:
  - type: web
    name: taskflow
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.20.2
      - key: PORT
        value: 3000
    autoDeploy: true
